NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
NEXT_PUBLIC_PRODUCT_API_MODE=mock
# Use "mock" until the Product API experiment endpoints are available.
# Authentication still uses NEXT_PUBLIC_API_BASE_URL in both modes.
NEXT_PUBLIC_PRODUCT_API_MODE=mock
